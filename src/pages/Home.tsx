import { Button } from "@mui/material";
import React from "react";

const Home: React.FC = () => {
  return (
    <div>
      <Button variant="contained">Click me</Button>
    </div>
  );
};

export default Home;
